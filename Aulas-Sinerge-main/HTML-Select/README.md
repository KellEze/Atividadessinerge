# HTML-Select (Select + input + total + troco)

Projeto conforme `HTML-Select/Intrucoes.md`.

## Como usar
- Abra `index.html` no navegador.

## Funcionalidades
- `select` escolhe o produto e preenche automaticamente o preço.
- `quantidade` recalcula o `total`.
- Campo `valor pago` + botão calcula o `troco` e mostra mensagem de insuficiência.

